diff --git a/app/src/display/main.c b/app/src/display/main.c
index e15e2de..6c62aad 100644
--- a/app/src/display/main.c
+++ b/app/src/display/main.c
@@ -140,6 +140,7 @@ int zmk_display_init() {
 
 #if IS_ENABLED(CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE)
 int display_event_handler(const zmk_event_t *eh) {
+    static bool started;
     struct zmk_activity_state_changed *ev = as_zmk_activity_state_changed(eh);
     if (ev == NULL) {
         return -ENOTSUP;
@@ -147,11 +148,17 @@ int display_event_handler(const zmk_event_t *eh) {
 
     switch (ev->state) {
     case ZMK_ACTIVITY_ACTIVE:
-        start_display_updates();
+        if (!started) {
+            start_display_updates();
+            started = true;
+        }
         break;
     case ZMK_ACTIVITY_IDLE:
     case ZMK_ACTIVITY_SLEEP:
-        stop_display_updates();
+        if (started) {
+            stop_display_updates();
+            started = false;
+        }
         break;
     default:
         LOG_WRN("Unhandled activity state: %d", ev->state);

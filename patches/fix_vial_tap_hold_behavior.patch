diff --git a/quantum/vial.c b/quantum/vial.c
index 84fed1ee18..5491d57503 100644
--- a/quantum/vial.c
+++ b/quantum/vial.c
@@ -344,8 +344,9 @@ static uint8_t dance_step(qk_tap_dance_state_t *state) {
         if (state->interrupted || !state->pressed) return SINGLE_TAP;
         else return SINGLE_HOLD;
     } else if (state->count == 2) {
-        if (state->interrupted) return DOUBLE_SINGLE_TAP;
-        else if (state->pressed) return DOUBLE_HOLD;
+        /* FIX_TAP_HOLD_BEHAVIOR (serach key for checking the patch applied. */
+        if (state->pressed) return DOUBLE_HOLD;
+        else if (state->interrupted) return DOUBLE_SINGLE_TAP;
         else return DOUBLE_TAP;
     }
     return MORE_TAPS;
